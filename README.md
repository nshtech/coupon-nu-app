# Willie's Wallet

To run in your iOS simulator:
    npx expo run:ios

To submit a new build to App Store Connect
    npx eas-cli build --platform ios --profile production
    npx eas-cli submit --platform ios --latest --skip-app-create